# E-Commerce Analytics - Python dependencies
#
# Pinned to the versions this project was actually built and verified with on
# Python 3.14.6 / Windows 11. Later versions will very likely work; these are the
# ones the 178 verification checks were run against.
#
# Install:  pip install -r requirements.txt

# --- data pipeline ---------------------------------------------------------
pandas==3.0.5
numpy==2.5.0
scipy==1.18.0

# --- reading the raw source ------------------------------------------------
# xlrd is required for the legacy .xls format the Tableau Global Superstore
# workbook ships in. openpyxl cannot read .xls and will fail with a confusing
# "File is not a zip file" error, because .xls is OLE2 and .xlsx is a zip.
xlrd==2.0.2
openpyxl==3.1.5

# --- charts ----------------------------------------------------------------
matplotlib==3.11.1
seaborn==0.13.2

# --- notebook --------------------------------------------------------------
jupyter==1.1.1
nbformat==5.11.1
nbclient==0.11.0
nbconvert==7.17.1
ipykernel==7.3.0

# --- NOT required at runtime ----------------------------------------------
# sqlite3  - standard library; used by src/verify_claims.py to execute the SQL
#            layer. No install needed.
#
# Power BI Desktop is required only to build the dashboard from
# dashboard/DASHBOARD_SPEC.md. It is not a Python dependency and nothing in the
# analysis pipeline needs it.
